app {
  server {
    host = "localhost"
    port = 8080
  }
  basic-auth {
    user = "admin"
    password = "password"
  }
  routes {
    ask-timeout = 5s
  }

  db {
    host = "localhost"
    port = "5432"
    database = "postgres"
    user = "postgres"
    pass = "qwert678Q"
  }

  security {
    salt = "HotPepper2023"
  }
}
